/*
import 'package:flutter_test/flutter_test.dart';
import 'package:pawprintgp/models/adoptionpost_model.dart';

void main() {
  test('AdoptionPost toMap and fromMap', () {
    final post = AdoptionPost(
      title: 'Test Title',
      description: 'Test Description',
      age: '2 years',
      breed: 'Test Breed',
      location: 'Test Location',
      userEmail: 'test@example.com',
    );

    final map = post.toMap();
    final newPost = AdoptionPost.fromMap(map);

    expect(newPost.title, post.title);
    expect(newPost.description, post.description);
    expect(newPost.age, post.age);
    expect(newPost.breed, post.breed);
    expect(newPost.location, post.location);
    expect(newPost.userEmail, post.userEmail);
  });
}
*/